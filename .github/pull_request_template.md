## What changed

<!-- One or two sentences. The diff already shows *what* — don't re-describe every line. -->

## Why

<!--
The actual reasoning: what was broken/missing, what caused it, why this fix
over another option you considered. If this closes an issue, link it — but
still explain the reasoning here, not just "fixes #123".
-->

## How this was verified

<!--
Concrete evidence, not "tested locally":
- Which command(s) you ran (flutter test, flutter analyze, manual repro steps)
- For a bug fix: how you confirmed the OLD code actually reproduces the bug
  and the NEW code doesn't (e.g. "reverted the fix, test failed with X,
  restored it, test passes")
- For a UI change: what you clicked through, on what device/simulator
-->

## Regression test

<!--
Link the test (file:line) that pins this behavior down, or explain why one
isn't practical here (e.g. pure UI polish with no logic branch to assert on).
"No test" should be a deliberate call-out, not a silent gap.
-->

## Follow-ups / known gaps

<!-- Anything intentionally out of scope, or a risk you're accepting. "None" is a fine answer. -->
